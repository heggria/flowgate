# 2026-09-15 第二轮完整复核

## 结论与范围

承接 2026-09-14 的修复提交 `767817e` 与报告提交 `55619b3`，在独立分支 `review/full-runtime-20260915` 继续审查。此次发现并修复 6 个可复现问题。主工作树、已安装应用及已批准的系统辅助服务保持原样。本轮复核完成后，仍不能将完整 v1 判为全部验收通过。

检查覆盖配置编译、DNS、节点引用、操作事务、原生启停与回滚、系统设置所有权、崩溃恢复、故障提示、更新与进程边界。自动测试使用隔离数据、隐藏且不可聚焦窗口；保持现有 VPN。

## 发现与修复

| 级别 | 可复现问题与影响 | 修复及证据 |
|---|---|---|
| P1 | 系统代理事务重写恢复记录时丢失内核 PID 与出生标识。若 helper 在此窗口崩溃，watchdog 无法凭内核身份恢复设置 | 保留当前内核身份。生产 `OwnershipJournal.swift` 的注入测试在修复前断言失败；修复后覆盖正常提交、提交失败后重新读取及按内核身份恢复。见 `work/review2/ownership-before.log`、`ownership-after.log` |
| P1 | 配置应用失败后忽略停止失败，继续启动回滚内核并覆盖旧进程句柄，可能留下无法通过正常停止清理的进程 | 清理失败进入未知且待恢复状态，保留句柄并阻止新应用；清理成功后才允许回滚，回滚自身失败也清理。生产 Swift 配合注入进程复现修复前 2 个存活、停止后仍剩 1 个；修复后覆盖清理失败、回滚失败和成功回滚三种路径。见 `rollback-before.log`、`native-rollback.log` |
| P1 | 域名形式的 DNS 服务地址能保存，但实际内核拒绝缺少域名解析器的配置；DoH URL 查询参数被丢弃 | 为域名解析配置 bootstrap 解析器并保留查询参数。实际内核完成域名 DNS 与 IPv6 字面量 DNS 的本地代理转发；域名 DoH 与外部网络 DNS 配置通过真实内核校验。见 `dns-forwarding.log`。DoH 校验不等于外部 DoH 实网成功 |
| P1 | 原生操作结果未知后，即使用户完成紧急断开，持久操作仍一直阻止重连 | 问题页增加“断开并重新核对”；只有停止标记匹配且具备相应模式的恢复权限，才解除未知状态。未知连接记录为失败并注明已恢复，不伪造连接成功；无特权手动后端不能清除旧特权操作的不确定性。实际 Service、原生组件及 UI 验证从禁止连接到显式恢复再重连。见 `recovery-flow.log` |
| P2 | 失败或未知操作的幂等重放返回正常 Promise，同一操作 ID 被其他方法复用时也返回旧方法结果 | 重放保持原失败语义，并拒绝跨方法复用；已成功重放仍正常返回。新增 3 项事务测试修复前均失败、修复后通过。见 `transactions-before.log`、`transactions-after.log` |
| P2 | 删除仍属于出口组的节点仅产生通用配置校验错误 | 同步移除多成员组引用，必要时选择剩余首个成员；删除最后一个成员或被规则源引用的节点时给出具体阻止原因。确认框说明影响；新增节点删除测试通过。不额外实现完整出口组编辑器 |

上述日志均位于此工作树的 `work/review2/`。新增自动回归已接入项目完整检查，原失败证据保留。

## 验证与实际边界

- 最终 `npm run verify` 单次完整通过（退出码 0），包含 101 项单元测试、类型与模块边界、构建、七协议真实本地转发、故障/更新/权限/恢复、UI 和完整产品流程；日志为 `work/review2/final-verify.log`。基础设施另有 3 项通过。
- 代码提交为 `8f94fef404dbbf50c43ef8c2a5d2d4501d7a7067`；重新构建清单记录 `dirty:false`。新包为 `outputs/FlowGate-20260915-review.app`，版本 0.3.2、构建号 `20260915.1`。包身份、签名完整性、隐藏启动、本地真实转发通过，见 `work/review2/package-result.json`。这是本地复核包，未使用 Developer ID 正式分发签名或公证。
- 新包读取用户配置的隔离副本（schema 2、48 个节点），使用真实所选节点验证：首轮 SOCKS5h 到 example.com、HTTP CONNECT 到 Cloudflare 返回 200；HTTP CONNECT 到 example.com 出现 curl 35（TLS 错误）。首轮不是全绿，见 `work/review2/real-result.json`。该结果尚不足以确定应用、节点或远端中的故障来源。
- 首次复测仍出现相同 TLS 错误（`real-retry-result.json`）；已安装版本的 3 项对照全部 200（`real-installed-result.json`）。随后新包连续执行 4 次 HTTP CONNECT 到 example.com、1 次 SOCKS5h 到 example.com、1 次 HTTP CONNECT 到 Cloudflare，6 项全部 200（`real-diagnose-result.json`）。新旧内核文件散列相同；不能凭后续成功抹掉前两次失败，亦未定位为确定的源码回归。保留为未定位的间歇实网失败，不声称稳定性验收完成。
- 所有上述实际应用测试均保持原用户数据散列、系统代理、DNS、默认路由前后一致。此前复核包已经不在原输出路径，因此对照使用当前已安装包；缺失路径的启动失败记录为 `real-baseline-result.json`，不计入网络测试。
- 原生回滚与系统设置恢复测试使用生产 Swift 实现和注入进程/系统后端，不是新 helper 的真实 root 崩溃实验。
- DNS 的 IPv6 本地回环成功不能证明外部 IPv6 可用。上一轮外部 IPv6 对照失败，记录在 `REVIEW_2026-09-14.md`，本轮不覆盖该结论。
- 2026-09-14 已安装 helper 的定向 TUN 转发成功不代表本轮新组件通过特权验收。新组件尚未安装或由管理员批准。
- 保留未完成项：当前 VPN 条件下的独占系统代理、整机 TUN 共存与特权崩溃矩阵、外部 IPv6、真实睡眠/切网/VPN/PAC 生命周期、长时间稳定运行及正式 Apple 签名分发。

本轮仅做本地复核、修复和包验证，不推送、合并、更新线上业务发布或替换已安装应用。

本次复核与已授权安全修复已完成。详细脱敏结果另存 `outputs/review-20260915/`；完整 v1 的上述剩余项与间歇实网失败继续保持未完成状态。
