# FlowGate 计划实现审计

审计日期：2026-09-10。按已批准的稳定 Shell、Service/Extension、原生 helper、Release Set/TUF 与内部网关方案核对。本文替换旧的累积状态表，历史审计保留在 Git 记录及交付的深度审计报告中；原需求与尚未验收的项目没有删除。

当前结论：主要开发缺口已修复，真实 TUF 源已部署并完成独立应用激活验收，最终组合的远程检查与分发仍以 PR 和版本记录为准。Apple 证书、公证和依赖签名的特权验收按用户要求延期；真实切网、系统睡眠和日常稳定性尚未通过，不能把隔离事件测试写成真机通过。

## 深度审计发现的 12 项缺口

| 项目 | 当前实现与证据 | 剩余边界 |
|---|---|---|
| 1. 迟发崩溃隔离与回退 | Service/Extension/Gateway/Renderer 共用持久故障归因与预算；启动健康后连续失败也隔离版本，恢复兼容版本；真实签名更新与重开应用测试 | 无兼容版本时显示固定恢复页；不回滚业务数据 |
| 2. 原生 unknown 与交接 | connect/disconnect 均按 operationId、revision 核对；结果未明拒绝更新安全点；handoff 写失败恢复旧 Service 和适配器准入；真实迟到结果及故障注入测试 | 不盲目重放副作用，特权路径仍待签名真机复验 |
| 3. 网络共存协调 | 真实系统代理 ownership、规范化本机端点、IPv4/IPv6 地址/路由冲突；观测变化串行重建已应用配置；冲突仅停止自己拥有的连接；外部恢复不抢占第三方设置 | 注入变化与实际本地转发通过，不等于 Wi-Fi/VPN 切换；PAC 可观察并保留，不执行 PAC 脚本 |
| 4. 睡眠/唤醒 | Main 和宿主暂停在途请求期限；拒绝暂停中新变更；恢复后重观测和原生核对；跨进程超时预算测试与实际转发通过 | 使用注入电源事件，尚未执行真实系统睡眠 |
| 5. 原生会话代次 | SessionLease 恢复阶段限制准入，旧清理重试不停止新代资源；生产 Swift 清理算法注入测试通过 | 正式 XPC/helper 特权会话仍待签名验收 |
| 6. 有序退出异常 | Renderer、宿主、原生和诊断各阶段独立清理、有限期限与错误记录，前一步异常不会跳过后续恢复；独占写锁等待旧 PID 回收 | 特权恢复失败与系统服务变化仍须真实复验 |
| 7. 无响应监督 | 周期健康检查、限时响应、挂起免误判、有预算重启；恢复先核对原生副作用 | 真实挂死及迟发崩溃测试通过；长期稳定性未完成 |
| 8. 网关真实出口 | 内部 Utility 网关通过 Service 解析选定出口，经原生代理访问上游；流式、取消、断线无直连回退和流中更新组合通过 | 是内部宿主验收能力包，不是完整模型网关产品 |
| 9. 插件与生命周期 | 诊断独立注册；sing-box 内核适配纳入清单、准入、排空与释放；UI/Service/Extension 事件携带版本组合、宿主代次、模块实例；UI 更新先释放旧模块 | 受信静态能力，不是任意第三方代码沙箱 |
| 10. 正式业务发布 | 已实现四角色隔离签名、不可变归档、preview 晋级 stable、单调撤回、旧新根双签及完整角色轮换、陈旧发布请求拒绝；真实 CLI/feed 整合由发布任务推进 | 实际线上仓库、更新激活证据以最终 RELEASING/ACCEPTANCE 记录为准，不能把本地签名测试当部署 |
| 11. 签名与完整应用更新 | 开发打包、产物哈希/来源校验、后台真实安装包转发测试、CI 开发预发布已具备 | Developer ID、公证、特权 helper、Squirrel.Mac 正式安装更新按用户要求延期 |
| 12. 真实网络与日常矩阵 | 本地七协议的 TCP/IPv6/DNS/UDP、故障和多出口真实流量已测；当前 utun5 环境新增外网 HTTPS 对照验收 | Wi-Fi/有线切换、真实睡眠、第三方 VPN/PAC 生命周期和长时间日常运行仍未验证 |

## 外网验收新增修复

手动模式原来总是设置 `route.auto_detect_interface=true`。在当前默认路由为 `utun5` 的环境，系统 curl 能访问的 HTTPS 站点经过应用时发生 TLS reset。只关闭该绑定后，HTTP CONNECT、SOCKS5h 到 example.com 以及 Cloudflare HTTPS 共三个请求全部成功；观测到的默认路由、系统代理和 DNS 均未变化。

实现改为仅 TUN 模式开启自动网卡绑定，手动与系统代理遵循系统路由；用户明确选择的外部接口仍保留 `bind_interface`。该配置依据 [sing-box 路由说明](https://sing-box.sagernet.org/configuration/route/) 的 TUN 环路防护语义。此次仅证明当前环境下的外网访问，不推断 VPN 所有者、出口公网 IP、真实切网或外部 IPv6 已验收。

## 已有产品闭环

节点编辑与内核测速、订阅导入/更新/管理、规则集 HTTPS 来源、规则与 DNS、配置修订/应用、主要表单草稿、连接和流量查看、四类 UI 贡献点、跨进程取消、加密凭据热轮换、持久任务/命名空间存储、UI 独立更新、Service 交接和失败回退均已有实现与自动验收。

最新 UI 统一页面、输入、选择器、弹层、空状态和主题；具有布局、键盘、可访问性和真实业务路径专项验证。完整模型目录/计费产品、多代网关滚动更新、第三方插件市场、其他操作系统原生版本及独立 Web 产品仍是原计划的后续范围。

## 验证口径

- `npm run verify`：类型、AST 依赖边界、单元测试、实际内核协议、宿主故障、签名更新、凭据、取消、电源事件、草稿与 UI 全套。
- `npm run test:infrastructure`：构建失败保留旧产物、产物来源/哈希及边界检查。
- 后台进程与实际安装包分别运行 `tests/background.integration.mjs` 和 `tests/package.integration.mjs`。
- 远程主分支有保护，需 Static checks 与 macOS verification 通过 PR；最新代码的远程结果应引用对应提交，不能用旧运行代替。
- 组合分支、最终应用和线上更新源分别验证。未完成或失败的项目在 ACCEPTANCE 明示，不写成已通过。
