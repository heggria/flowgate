# FlowGate 计划实现审计

结论：完整 v1 尚未达到系统接管与正式分发验收条件。当前实现进展见第 9 节；第 1–8 节为保留的历史审计。多协议、管理闭环、宿主能力和更新链路已持续补齐，但真实系统代理/TUN、第三方 VPN、睡眠唤醒与正式发布仍须完成，不能标记原计划全部完成。

审计基线为已批准的《FlowGate 架构修订：主进程、LLM 能力包与独立更新体系》完整计划，以及项目 REQUIREMENTS.md、ARCHITECTURE.md、RELEASING.md、ACCEPTANCE.md、README.md。界面部分同时核对后续设计调研。初次检查时间为 2026 年 9 月 9 日；后续实施进展见本文件第 8 节。下文源码路径相对仓库根目录。

本次读取实际实现和已有测试结果，没有为了文档审计重新改变系统网络或重复执行全部测试。此前 20 项单元测试、类型检查、构建、界面和更新测试通过，只能证明这些测试覆盖的行为，不能推出计划全部完成。下表按可验收事项拆分，不用未经工作量加权的百分比描述进度。

## 1. 代理产品与网络共存

| 计划事项 | 状态 | 实际实现、证据与剩余工作 |
|---|---|---|
| 独立手动 HTTP 代理与启停 | 已实现并实测 | `packages/domain/src/configuration.ts`、`native/NetworkEngine.swift`；`tests/native.integration.ts` 实际请求经过 sing-box 并返回正文 |
| SOCKS 入站与多协议节点 | 部分实现 | mixed 入站与节点协议编译存在；不能用 HTTP 实测代替 SOCKS、Trojan、VLESS 等逐协议连接验证 |
| 订阅导入和刷新 | 基础已实现 | `packages/extensions/src/parser.ts`、`packages/service/src/core.ts`；支持若干格式、HTTPS 下载、刷新保留节点 ID；不等于导入完整原配置的所有语义 |
| 节点与订阅的完整管理 | 部分实现 | 可导入、选出口、移除节点和刷新订阅；缺节点编辑/测速、订阅重命名/移除及完善错误状态等日常管理闭环 |
| 规则与 DNS | 部分实现并局部实测 | 四类规则、默认出口、配置校验、DNS 与外部接口独立解析器已编译；实测覆盖 HTTP 与 IP 直连分流，未覆盖完整 DNS/IPv6/UDP 矩阵；规则集来源与更新未落地 |
| 系统代理 / TUN | 有代码，未验收 | Swift helper 和 SystemConfiguration 所有权恢复已编译；签名安装、管理员批准、真实接管与恢复未验证 |
| 外部 HTTP 代理作为出口 | 已实现并实测 | `tests/native.integration.ts` 验证外部 HTTP 出口及指定规则直连 |
| 外部 SOCKS / VPN 接口作为出口 | 部分实现 | 配置支持 SOCKS 与 bind_interface，接口配独立 DNS；不能宣称所有外部软件、VPN 或强制隧道兼容 |
| PAC 与现有系统代理协调 | 部分实现 | `native/OwnershipJournal.swift` 检测已有代理/PAC 后拒绝接管、保留原设置；没有 PAC 策略导入和完整协同模型 |
| 外部网络变化自动协调 | 未完整实现 | `network.refresh` 为显式检查，UI 首次读取时自动检查；唤醒只发布快照。未见持续网络/外部进程变化订阅及自动重协调 |
| 循环、串联、地址冲突与强制 VPN 判定 | 未完整实现 | 部分输入约束和内核校验不能替代统一冲突分析、路径循环检测和限制提示 |
| 日志与失败诊断 | 部分实现 | 操作记录、内核状态、连接记录可见；缺可查询的统一生命周期追踪与系统故障诊断闭环 |

## 2. 稳定外壳与服务

| 计划事项 | 状态 | 实际实现、证据与剩余工作 |
|---|---|---|
| Main / Service / Extension 分进程 | 已实现 | `src/desktop/main.ts`、`packages/service/src/host.ts`、`packages/extensions/src/host.ts`；业务实现未直接装进 Main |
| 单实例、窗口与托盘 | 已实现 | 单实例锁、窗口重开、托盘与关闭窗口保留服务；深链处理尚未实现 |
| Renderer / Service 故障隔离 | 主要路径已实测 | E2E 验证重载/Service 强杀后内核 PID 与实际转发继续；Renderer 真崩溃、Extension 崩溃和预算耗尽的完整组合未验收 |
| IPC 身份、代次与超时未知 | 基础已实现 | `packages/shell/src/supervisor.ts`、contracts、preload；有会话、epoch、方法限制、operationId；无通用端到端请求取消协议，响应结构验证也未全面统一 |
| 单写存储与操作串行 | 已实现并测试 | `packages/service/src/store.ts` 有独占锁、串行事务、原子文件替换；测试覆盖去重和排空 |
| 期望 / 实际 / 已应用协调 | 部分实现 | 配置 revision、native appliedRevision、重启时 operationId 核对；未形成外部事件驱动的完整持续协调器 |
| 原生租约与恢复日志 | 有实现，部分未验收 | 手动模式独立内核已测；特权 XPC 会话、所有权 journal、异常恢复有代码，待签名真机故障验收 |
| 有序退出与恢复 | 部分实现 | 先停止宿主、再 native close；helper 按拥有字段恢复。完整退出故障、恢复失败重试、辅助服务失联场景未完成验收 |
| 睡眠 / 唤醒 | 部分实现 | 有 resume 回调重新发布；未覆盖暂停中的时限、租约与网络重建专项测试 |
| 大文件验证不阻塞 Main | 未按计划完成 | `packages/release/src/loader.ts` 在稳定进程读文件并同步计算 SHA-256；未移至受监督任务 |
| 固定恢复页面 | 已实现 | `src/desktop/recovery.*`；具备恢复和紧急操作入口，复杂恢复场景仍依赖后续故障测试 |

## 3. 模块与未来网关的宿主能力

| 计划事项 | 状态 | 实际实现、证据与剩余工作 |
|---|---|---|
| React 静态模块、依赖 DAG、原子贡献 | 已实现并测试 | `packages/runtime/src/lifecycle.ts` 与 `src/ui/modules.tsx`；依赖排序、重复检查、失败释放 |
| 页面 / 命令 / 设置 / 详情贡献点 | 部分实现 | Contribution 类型包含四类，但 UI 实际主要消费 route；设置、详情页目前仍为静态组件，不是完整统一贡献消费机制 |
| 解析 / 发现 / 内核通用插件生命周期 | 部分实现 | Extension Host 的订阅解析分支与旧诊断 Registry 并存；不是所有能力统一走 manifest、依赖、激活、停止的完整框架 |
| services 能力 | 部分实现 | CapabilityHost 可管理 ServiceLifecycle 对象；独立网关进程由测试脚本 fork，尚无应用中按受信清单启动任意内置服务入口的闭环 |
| endpoints / streams 能力 | 部分实现 | MockGateway 自己监听回环 HTTP、处理背压和取消；缺宿主统一端口分配、占用登记与资源所有权接口 |
| egress 能力 | 契约和 mock 验证 | 当前测试比较出口 ID，并未让网关通过选择的真实代理访问上游；不能等同真实出口关联 |
| credentials 能力 | 基础作用域实现 | 凭据引用白名单和回调存在；缺正式凭据存储、轮换及完整服务接入 |
| storage 能力 | 基础实现 | NamespacedStorage 可读写 JSON；未形成迁移、并发写协调和故障恢复闭环；包 ID 的点路径校验还需加强 |
| jobs 能力 | 部分实现 | 可启动/取消；无暂停、持久检查点、重启恢复。drain 等待所有任务，但任务不响应取消时没有强制期限保障 |
| observability / releaseLifecycle | 部分实现 | 有 TraceBuffer 与生命周期方法；并未把各服务、插件、更新阶段统一串起来 |
| 内部流式测试服务 | 已实现并局部测试 | 工具 JSON 分块、未知事件、流内错误、取消、慢客户端背压、usage unknown、排空有测试 |
| 测试网关注册设置页和服务入口 | 未完成应用集成 | 有测试 manifest，无应用中的网关设置贡献和受宿主管理的启动入口 |
| 网关流中更新 / 失败回退 | 未完成组合验收 | 网关排空和普通 Service 更新分别测试；尚未把网关能力包放进 Release Set，在流中升级及回退 |
| 预算并发与用量 | mock 基础实现 | 内存预留和 unknown 保留存在；不等于持久、多实例用量账本；完整并发场景覆盖不足 |
| 完整模型网关产品 | 按计划后续 | 模型目录、Provider、路由、价格、真实模型协议不属于首版交付缺陷；首版宿主验证本身仍有上述缺口 |

## 4. 动态更新与发布

| 计划事项 | 状态 | 实际实现、证据与剩余工作 |
|---|---|---|
| TUF 元数据和完整文件验证 | 已实现并局部实测 | `packages/release`；签名、摘要、入口、兼容、路径逃逸/链接/多余文件防护及离线签名回执测试 |
| Release Set 精确组合 | 已实现 | UI、Service、Extension 入口与文件哈希、schema/protocol 范围；内置包只允许已列出的 ID |
| UI 独立更新保留代理 | 已实测 | 本地 HTTPS / Ed25519 TUF 测试验证 UI 切换保持 Service 与内核 |
| Service 排空、切换和失败回退 | 主要路径已实测 | `UpdateCoordinator` 和真实 Electron 测试验证；配置事务排空有单测，但并发实际 apply + 升级的组合仍未完整实测 |
| 所有编辑草稿恢复 | 部分实现 | 仅订阅文本存入 Main 内存；规则表单、DNS/端口草稿等未纳入安全点，UI 更新会丢失未保存输入 |
| 版本化持久交接记录 | 部分实现 | 有业务状态、版本状态和进程代次；切换现场部分状态在 Main 内存，未完成原计划明确的版本化交接协议 |
| 八类版本及追踪 | 部分实现 | 有 Release Set/协议/schema/内置包版本；Service trace version 仍写死 0.2.0，UI/壳/插件实例版本未全面关联 |
| 目录内容、权限变更与撤回 | 部分实现 | 现为通道 release.json + 简化 manifest；缺完整权限/贡献类型/发布说明模型、权限提升处理和已安装撤回版本提示策略 |
| 正式官方目录 | 未部署 | `src/desktop/update-config.json` enabled=false，metadataUrl/targetUrl/applicationFeed 为空；本地测试源不等于正式源 |
| stable / preview | 基础模型 | 通道字段与检查参数存在；无完整用户通道选择及发布运维闭环 |
| 密钥轮换、过期与重放演练 | 部分完成 | 使用 TUF 机制并保存信任链；没有覆盖全部轮换/撤回/时钟与过期场景的验收矩阵 |
| 完整应用更新与签名公证 | 适配器存在，发布未完成 | 有 autoUpdater 适配器、开发打包；正式嵌套签名、JIT 权限、公证、完整应用 feed 未完成 |
| CI 和官方发布流水线 | 未建成 | 有本地 verify/check-boundaries 和发布说明；未发现 .github 工作流，不能把本地命令称为已接入 CI |

## 5. 界面完成度与细节

新版概览、真实流量曲线、连接列表/搜索/详情、启停、浅深色与窄窗无横向溢出已完成并经过界面实测。它取代了旧首页口号与工作区装饰，但未意味着所有页面都已达到完整工具的精细度。

需要继续优化的具体细节：

1. 配置页统一显示待保存、已保存、待应用，失败时保留输入；目前规则提交后会提前清空输入，保存失败时不利于修正。
2. 草稿覆盖所有表单，并作为更新安全点，而非只保存订阅文本。
3. 当前图表较短采样窗口会留白，这是诚实呈现；可以用更清楚的采样起点与数据中断标记改善理解，不添加假数据。
4. 连接列表可补排序、出口的人类可读名称和长字段查看方式，改善真实节点较多时的使用效率。
5. 节点/订阅补状态、更新时间、更新失败和管理操作；当前仍较基础。
6. 更新操作增加检查结果、阶段、失败原因及当前版本的直接反馈。

这些是审计后的待办，本轮没有再次修改 UI 或声称这些细节已经完成。

## 6. 实施优先级

**第一优先：补齐核心工具闭环。** 完善节点/订阅管理、编辑草稿与配置状态；补外部网络持续观察、冲突判定与重协调；完成系统代理/TUN 的正式签名测试路径。可本地开发的部分无需等待证书，真实系统验收需要相应签名和批准。

**第二优先：兑现宿主可扩展承诺。** 统一插件注册与贡献消费、服务入口/端口/出口资源接口，补 jobs 的期限和恢复，让内部网关真正经过应用的能力包启用、流中排空、升级及回退流程。此阶段不扩展成完整 LLM 产品。

**第三优先：追踪和交付。** 补版本及跨进程追踪、更新交接记录、签名流水线与 CI、官方元数据和权限模型，再完成正式更新源及故障矩阵。

正式发布依赖：Apple Developer ID、签名/公证配置、官方 HTTPS 分发地址和独立 TUF 密钥。这些依赖不应掩盖前面仍需要编写的功能。

## 7. 测试证据与解释

- `work/e2e-result.json`：Electron 44.3.0；2026-09-09T10:20:35Z，导入、规则、预览、重载、Service 崩溃隔离等。
- `work/ui-result.json`：2026-09-09T10:21:09Z；18 个真实采样点、9,233,932 字节下载、搜索/详情/主题/窗口验收。
- `work/update-result.json`：2026-09-09T10:21:19Z；UI、Service、失败回退三条真实更新路径。
- `work/native-result.json`：2026-09-09T09:53:34Z；手动 HTTP、外部 HTTP、IP 分流、错误配置保护。
- `work/gateway-result.json`：2026-09-09T09:55:55Z；独立 mock 服务、流式事件、排空和未知用量。
- `tests/*.test.ts`：20 项单元/局部集成测试；覆盖范围见测试名。界面、原生与更新 E2E 不计入这 20 项。

已有文档存在口径偏差：REQUIREMENTS.md 的“当前只有可信诊断插件”过时；ARCHITECTURE.md 某些宿主能力描述过于笼统；ACCEPTANCE.md 把开发缺口与长期验收混在一起。应以本审计的逐项状态作为下一阶段任务输入，并保留原计划要求，不能通过缩减文档来宣布完成。


## 8. 授权继续实施后的进展

本节更新初次审计的状态，不将仍未完成的项目改称已完成。代码与本地行为验证已推进如下：

| 初次缺口 | 当前实现与验证 | 仍有边界 |
|---|---|---|
| 节点与订阅管理 | 节点名称/地址/端口编辑、修订冲突检查、订阅重命名和移除、刷新失败状态；移除来源保留本地节点与规则 | 未增加节点测速、规则集订阅 |
| 全部主要编辑表单草稿 | 稳定外壳按键保存；规则、设置、外部网络、路由预览、节点编辑与订阅名称/内容；失败保存保留输入；拒绝草稿写入时延后重载/更新 | Main 进程退出后的草稿恢复未承诺；该设计针对界面重建 |
| 网络持续观察 | 每 15 秒合并观测，失败显示旧状态，停止后忽略迟到结果；缺失外部接口、已有代理、默认隧道提示 | 观察不等于自动修改第三方路由；完整 PAC、强制 VPN 与签名 TUN 共存仍未验收 |
| 生命周期并发与期限 | 服务启动占位、排空取消启动、禁止迟到激活、限时清理；监督器失败也终止宿主；部分停止后的更新恢复 | 仍需特权 helper 故障矩阵 |
| 扩展生命周期 | 解析和网络发现通过统一 ModuleRuntime/ExtensionRuntime 注册与停止；下载传播取消 | 旧诊断插件内部仍保留专用 Registry，不是不可信代码沙箱 |
| jobs / storage | 命名空间串行事务、原子迁移；持久任务检查点、暂停/取消、恢复策略；不可安全重放的运行中任务恢复为 unknown | 当前无真实模型目录/计费业务，任务需自行声明可安全恢复 |
| endpoints / streams | 宿主登记回环端口和流、限制重复占用、退出释放；网关使用这些接口 | 多代服务滚动切换按原计划后续 |
| egress | 真正通过选定 HTTP 代理到本地上游的测试；拒绝越权，代理失败无直连回退 | 当前测试传输不支持 HTTPS/SOCKS，不代表完整模型出口实现 |
| 内部网关应用与更新 | 受监督 Utility Process、设置贡献入口、Release Set 文件；内部测试开关启用；流中 UI/Service 更新与失败回退真实验收 | 默认产品不显示内部测试入口，不交付完整 LLM 网关 |
| 大文件验证 | 固定外壳独立校验线程、内存限额、超时/崩溃退出；真实签名更新用此路径 | 正式 Apple 签名仍独立验收 |
| 版本与交接 | Service/外壳版本取自构建元数据，扩展信息取自健康握手；持久版本化交接记录检查修订不倒退；更新阶段持久记录 | 未完成每条 IPC、插件与内核的全链路 trace 关联 |
| 目录与权限 | 发布说明、内置 ID/版本/贡献类型检查，超出固定能力范围拒绝动态更新 | 已安装版本的远程撤回策略、全部密钥轮换矩阵仍未完成 |
| CI / 正式打包 | 已添加固定 action commit 的 macOS 验证工作流；嵌套签名/JIT 与钥匙串公证入口 | 没有远程 CI run；没有 Developer ID/公证凭据，不声称正式签名验证通过 |

继续未完成的开发事项：节点测速和规则集来源、完整贡献消费点、完整端到端取消/trace、正式凭据轮换接入、已安装撤回版本处理、更多代理协议/IPv6/UDP/DNS组合测试。系统代理/TUN、正式更新源与 Apple 分发还需要现有环境未提供的证书、发布配置和真实原生验收。当前仍不是原计划全部完成。

## 9. Goal 持续迭代的当前结果

本节为当前状态，第 1–8 节保留初次审计与上一阶段的历史，不能再将其中旧的“未实现”直接当作当前结论。原计划的系统代理/TUN、正式签名分发和真实 VPN 共存验收仍未取消，因而完整 v1 **尚未完成**。

| 原有缺口 | 本阶段实现与证据 | 当前边界 |
|---|---|---|
| 节点测速 | 通过内核 API 发起 URL test；只接受本轮新测量，支持取消和并发限制；真实请求经过选定出口 | 延迟取决于测试站点；取消等待不保证内核已经停止探测 |
| 规则集来源 | HTTPS 域名列表／单条件 sing-box JSON 导入、刷新、移除；失败保留上一快照；实际界面测试 | 不支持的组合条件明确拒绝，避免改变原语义 |
| 全部贡献点 | 页面、命令搜索、设置页、连接详情均消费贡献注册；贡献携带模块身份 | 首版受信静态模块，不是第三方沙箱 |
| 取消与追踪 | Renderer→Main→Service→Extension 取消；已提交操作结果需核对；统一 traceId、宿主代次、模块实例；诊断有界持久化 | 流量数据不进入控制日志，未记录订阅正文/URL |
| 凭据 | Main 使用 Electron safeStorage 加密，能力包按引用获取；代次冲突保护、热轮换和重启恢复真实验证 | 内部网关验证 provider.mock，未扩展为完整模型管理产品 |
| 更新目录 | publisher/platform/components/权限/贡献类型/发布说明；stable/preview；签名撤回名单单调持久化；已撤回代码下次启动拒绝，当前运行提示处理 | 正式 HTTPS 仓库、角色密钥与应用 feed 尚未配置 |
| 信任轮换 | 新旧根双签验证、缺失根链拒绝、退役密钥拒绝、过期禁止激活且保留已验证离线版本 | 这是隔离密钥演练，不能代替正式密钥管理 |
| 多协议与 DNS | SOCKS、Shadowsocks、VMess、VLESS、Trojan、Hysteria2、TUIC 的 TCP、IPv6 目标、DNS、UDP 真实本地矩阵 | 不是外网所有实现、传输变种或强制 VPN 的兼容承诺 |
| 网关 egress | HTTP / HTTPS CONNECT / SOCKS5h，明确目标范围、TLS 验证、失败不直连回退 | 不执行 PAC 脚本或读取环境代理来决定出口 |
| 外部网络 | 持续观察系统 HTTP/SOCKS、PAC、IPv4/IPv6 路由；手动选入出口；独立接口与 DNS 编译；外部断线后保留失败策略 | PAC 动态结果不能当作单一出口；真实 VPN/TUN 仍待签名环境验收 |
| 原生异常 | 独立 kernel watchdog 按 PID 出生时间防误杀；桥接强杀清理遗留内核；失联禁止写入；helper 会话结束的有限恢复重试 | 特权路径尚未真机运行；手动模式真实强杀通过 |
| 系统所有权 | 可注入配置后端，测试 PAC 保留、第三方部分修改、提交失败重试、持久恢复和服务消失 | 注入测试不修改系统设置，也不替代特权验收 |
| Renderer 故障 | 重建窗口/页面，托盘和服务不随最后一个窗口销毁退出；真实 HTTPS 在 Renderer/Extension 强杀后保持 | 仍需长时间日常使用与真实睡眠/唤醒验收 |
| Service 更新并发 | 配置 apply 已处于 pending 时启动签名更新；完成后交接；重复操作不再次应用 | 普通更新只接受兼容 schema，不做破坏性迁移 |
| 深链 | flowgate://open/页面；只导航白名单页面，拒绝查询参数和控制动作 | 正式 URL Scheme 随应用打包注册 |

新的完整验证入口为 `npm run verify`，包含上述协议、凭据、取消、功能界面和所有权测试。完整入口本地通过（42 项单元/局部测试和所有集成场景），证据为 `work/goal-final-verify.log` 与各 `work/*-result.json`。随后仅修正规则页标签，类型检查、构建和功能界面再次通过。

目前无法独立完成的交付条件：本机 `security find-identity -v -p codesigning` 仍为 0 个有效身份；官方 HTTPS 更新地址、正式 TUF 信任根与 Squirrel.Mac feed 未提供；仓库未发布到远端，无法提供远程 CI run。已向用户请求证书名称、公证配置名和公开分发/信任根路径，没有索取密钥。获得资源后还须实际完成签名安装、管理员批准、系统代理/TUN、第三方 VPN、睡眠唤醒及崩溃恢复验收。不能将当前构建标记为全部计划完成。
