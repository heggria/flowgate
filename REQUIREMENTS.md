# FlowGate 产品需求基线

## 用户确认的定位

面向整机流量的插件化代理客户端。第一版同时交付自身完整代理能力与其他代理软件的共存协调能力。支持范围按网络能力和接入机制设计，不按 Tailscale、GUI.for.SingBox 等品牌写死；这些仅为本机测试样本。网络流量是一等公民，LLM 是可选专属处理类别，不能把产品收窄为 Agent 网关。

## 第一版两条必需主线

### 自身代理能力

基于 sing-box 完成配置/订阅导入更新、节点与出口管理、启停和状态、系统代理/TUN 接入、规则分流与 DNS、日志与失败诊断、配置校验和恢复。以上为拟定功能拆分，细节需在产品设计中确认，不能把仅有诊断或预览视为第一版完成。

### 外部代理共存

对外部 HTTP/SOCKS 代理、系统代理/PAC、VPN/TUN 接口等接入形态建模；按实际能力区分可观察、可用作出口、可协同配置。识别路由、DNS、代理设置的所有权与冲突，为不同流量选择本应用或外部网络路径。避免循环和意外串联；处理外部软件启动、退出、重连、网络切换与地址冲突。识别不允许分流的环境并说明具体限制，不承诺无条件兼容任意强制 VPN。

## 插件体系目标

- 内核适配：sing-box 的配置、生命周期和控制能力。
- 资源来源：订阅格式、配置来源和规则集合。
- 外部网络提供者：按通用协议/系统接口接入，必要时增加具体软件适配器。
- 流量策略与协议扩展：分类、出口策略，后续 LLM 专属能力。
- 插件运行时：清单、API 版本、注册、依赖、激活、停用、清理和故障处理。

核心统一负责策略冲突、凭据与系统配置权限、变更事务和恢复；插件不能各自随意修改全局路由/DNS。目前订阅解析、规则解析、网络发现与两个可选应用诊断均已纳入统一生命周期，并提供扩展管理页面、持久启用偏好和签名版本组合更新。原生内核与系统权限边界仍随完整应用交付，不能通过扩展页面安装任意原生插件；具体范围见 EXTENSIONS.md。

## 验收方向

1. 不依赖外部代理，本应用自身能完整建立代理并执行分流。
2. 增加一种资源解析或外部代理适配实现，通过稳定扩展点注册，避免在核心按品牌增加分支。
3. 同时运行外部代理时，指定流量到达各自预期出口；验证 DNS、IPv4/IPv6 与失败策略。
4. 外部代理断线/重连及本应用退出后，系统状态按所有权恢复，不能覆盖其他软件更新。
5. UI 的连接成功不能代替真实请求/出口验收；诊断通过不能代替代理完成。

逐项实现状态见 [PLAN_AUDIT.md](PLAN_AUDIT.md)，原需求不因当前实现较窄而取消。

## 当前实施基线

用户已确认 Electron + React 和 Codex 视觉参考，并批准稳定 Shell、独立 Service/Extension、Release Set/TUF、原生 helper 与内部网关能力包方案。正在按该方案实现，详见 ARCHITECTURE.md。验收与外部阻塞见 ACCEPTANCE.md；原只读原型不作为完成标准。
