# 2026-09-16 长测失败与测试清理修复

CI1b build 101（产品提交 `1b635cb`）的八小时隔离长测失败，不能作为完整时长或真实睡眠验收。此前 `6d967e4` 的两套完整 CI 均通过；其清理控制检查未覆盖自动化连接断开的情况。

## 本轮实际结果

- 运行编号 `b9017941-a9eb-4176-b4a9-259a24e4c2ab`，请求28800秒，20761秒结束；最后成功样本20751秒。2061次采样、343次启停、171次页面重载、4126次请求，最大采样间隔11秒。
- 错误为 `Target page, context or browser has been closed`，最终 `networkUnchanged=false`。相邻系统记录包含合盖睡眠，但直接因果关系尚未确定。旧工具未保存网络字段的原始前后快照，不能推断变化由应用造成或已全部恢复。
- 一次协调重建有匹配的成功网络操作、旧内核退出和再次转发证据，涉及默认接口/网关和路由字段；这不改变整轮失败结论。
- 后续发现测试应用、内核及运行器仍在。核对其精确隔离身份后，通过应用正常退出流程清理，捕获的进程全部退出，清理前后网络快照一致。这不能改判原始网络变化结果。

原件与分析：`work/readiness/ci1b-soak-8h-result.json`、`ci1b-soak-failure-analysis.json`、`ci1b-soak-residual-cleanup.json`。内存采样只说明观测值，不证明没有泄漏。

## 修复与验证范围

隔离回归主动关闭 Chromium 的自动化连接，复现旧工具的关闭调用返回后应用与内核仍存活。证据为 `work/readiness/soak-disconnect-before.json`，复现后已正常清理。

测试工具现在捕获原始子进程及本地调试端点，关闭后实际等待退出；失联时核对 PID、可执行文件和隔离目录，再走应用正常退出。若必须强制终止，单列为非正常清理并判失败。原始失败结果不被后备清理覆盖。报告新增生命周期事件和网络变化字段，私有原始网络快照不进入公开报告。

实际 build 101 的正常完成、协作式停止、主动断开测试连接后三项控制验证通过：后两项运行结果按预期仍为失败，应用和内核均退出，网络前后不变。113项单元测试及类型检查通过，新增遥测后的三项最终复验也通过，证据为 `work/readiness/soak-disconnect-final-control-result.json` 与相关日志。本次提交的完整 CI 以 `work/STATE.md` 为准。

完整八小时、真实睡眠/切网、完整 VPN 共存、外部 IPv6、正式签名/公证和完整应用更新仍待完成。候选仍为开发包，PR保持草稿，没有合并或正式发布。
