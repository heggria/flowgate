# 2026-09-14 全需求与实际运行复核

## 结论与范围

审查基线为主分支 c2d8424、已安装 `/Applications/FlowGate.app` 0.3.2。修复在独立分支 `review/full-runtime-20260914`，保留其他工作树。当前已有可运行的代理能力，但不能判为全部 v1 需求验收完成。

用户要求保持现有 VPN。全套自动测试使用隐藏、不可聚焦窗口和隔离数据；实网测试使用用户配置的独立副本，不修改原配置。TUN 实验仅路由测试地址 `198.18.0.88/32`，不接管其他流量。

## 发现与修复

| 级别 | 问题与实际影响 | 修复和验证 |
|---|---|---|
| P1 | 原生 watchdog 在内核先退出时直接返回，可能漏掉已拥有系统代理设置的恢复 | 无论内核何时退出均进入恢复；检查原 journal 的 PID、出生标识与代次，恢复过程再次持锁核对。生产 Swift 注入测试覆盖已退出、PID 重用、新代 journal、手动模式。真实 root 崩溃恢复尚未复验 |
| P1 | 0.3.2 未包含此前的侧边栏问题中心；错误仍占页面顶部，后续无关操作会清掉错误 | 恢复侧边栏入口、独立问题页、短时浮层和恢复链接，保留新版外观、紧凑布局、草稿提示、所选/生效出口以及免费 helper 安装。界面验证覆盖不挤动页面、关闭浮层不丢记录、跳转和成功后历史保留 |
| P1 | 自动连接等后台操作失败未经过前端请求捕获，内核仍为 stopped 时用户可能看不到失败原因 | 从持久操作快照恢复最近失败/未知结果，与前端请求及内核状态合并去重。新增单元和 UI 验证：后台失败在 stopped 状态可见，重复推送不重复计数 |
| P2 | 固定恢复页读取状态和恢复内置版本的失败未捕获，点击后可能没有反馈 | 补充明确失败原因、脱敏、处理中状态和按钮恢复；生产脚本模拟失败验证通过 |
| P2 | 部分 UI 测试假设启动必为浅色，与跟随 macOS 外观冲突 | 测试显式设置隔离应用的初始外观；不修改系统外观 |
| P2 | 验收文档仍把所有特权模式归为证书延期，已与 0.3.2 本机管理员批准 helper 的实现冲突 | 更新验收口径；本机免费 helper 与 Developer ID 正式分发分开记录 |

## 本轮实际网络证据

- **已安装应用 + 真实已选节点**：读取 schema 2、48 个节点的配置副本；HTTP CONNECT 到 example.com、SOCKS5h 到 example.com、HTTP CONNECT 到 Cloudflare 三个 HTTPS 请求均返回 200，TLS 验证开启。
- **真实已安装 helper + 定向 TUN**：建立 utun100，指定测试地址的请求实际通过受控出口；停止后接口移除，系统代理、DNS、默认路由与测试前一致。仅证明此定向组合，不等于整机 TUN 的全部 VPN 共存路径。
- **VPN 冲突保护**：现有 VPN 为默认接口时，系统代理启动被明确拒绝；内核未留存、系统代理未变化。这是保护通过，独占系统代理转发仍未复验。
- **依赖审计**：默认直连 npm 审计端点不可达；通过已验证的本地代理重试成功，返回各级已知漏洞计数均为 0。此为本次审计响应，不代表没有未知漏洞。
- **外部 IPv6 未通过**：对同一 Cloudflare AAAA 地址保持原域名和 TLS 校验进行对照，系统直连 curl 返回错误 7，所选代理节点返回错误 35。不能将此归结为已证实的单一应用缺陷，也不能判为 IPv6 外网可用。记录见 `work/ipv6-result.json`。
- 以上过程原始用户配置散列、系统代理、DNS、默认路由前后对比通过。未关闭 VPN，未改变系统安全设置。

详细脱敏结果：`work/installed-real-result.json`、`work/local-acceptance/tun-scoped-result.json`、`work/local-acceptance/vpn-guard-result.json`、`work/audit-retry-result.json`、`work/dependency-audit.json`。

## 需求覆盖与剩余边界

| 需求 | 本轮证据 | 判定 |
|---|---|---|
| 独立代理内核、启停、HTTP/SOCKS 入站 | 本地协议真实转发与已安装应用所选节点外网 HTTPS | 通过已测路径 |
| 系统代理 | 实际 VPN 冲突拒绝；生产所有权算法注入测试 | 当前 VPN 条件下不支持接管；独占实网未复验 |
| TUN、VPN 共存 | 已安装 helper 定向 TUN 真实转发及退出恢复 | 局部通过；整机接管、多 VPN 组合未全部验收 |
| 订阅/完整配置/节点和出口 | HTTPS 订阅、七协议本地转发、当前持久 schema 2 数据启动 | 已支持格式和配置子集；不兼容资源必须提示，不能宣称任意配置全兼容 |
| 规则、DNS、IPv4/IPv6、UDP | 本地协议/规则/失败策略矩阵 | 本地通过；本轮外部 IPv6 对照均未通过 |
| 外部 HTTP/SOCKS 与强制 VPN/PAC | 通用出口、实际本地转发、冲突/所有权测试 | PAC 仅观察保留；不能无条件兼容强制 VPN |
| 热更新/独立业务发布 | 实际签名更新、交接、回滚与失败矩阵 | 本地验证与线上当前发布是不同证据；本轮补丁尚未线上发布 |
| 模块边界、插件生命周期 | 类型/AST 边界和扩展故障、停用、清理矩阵 | 受信内置能力；不是任意第三方插件沙箱 |
| UI、草稿、错误、macOS 集成 | 交互/布局/状态/无障碍专项与实际包隐藏启动 | 以本轮日志为准；错误中心修复不在原已安装 0.3.2 中 |
| 特权崩溃与恢复 | 原生实际 SIGKILL、生产 watchdog 注入与定向 TUN 正常退出 | 修复版特权 helper 的真实强杀矩阵未复验 |
| 睡眠/切网/外部软件重连 | 注入事件与本地生命周期测试 | 真实睡眠、Wi-Fi/有线切换、VPN/PAC 启停未验收 |
| 日常可靠性、正式分发 | 自动回归与短时实际请求 | 长时间运行、Developer ID/公证/完整应用正式更新仍未完成 |

## 构建身份与最终回归

执行中，完成后更新。不得将旧安装包网络结果直接归给修复版。重新构建原生组件后，系统辅助服务必须重新由管理员批准该构建，旧安装的批准不会自动继承。
