# FlowGate 持续迭代与发布标准

目标：持续发现、复现、修复并验证问题，使产品行为、界面、文档与发布产物保持一致，完成原需求中的代理能力和共存能力，再判断是否可以对外正式发布。不能承诺没有任何缺陷，也不能用一次测试通过代替真实使用验收。

## 工作方式

1. 以 REQUIREMENTS.md 为范围，保留既定架构和 Codex 外观参考；不通过删除未完成功能缩小验收标准。
2. 每轮优先处理数据损坏、权限/系统网络恢复、无法连接、无法恢复和发布错误，再处理功能完整性与体验不一致。
3. 对缺陷先建立可复现证据，再做聚焦修改；测试覆盖实际影响，修复后更新对应状态。已通过且未受新改动影响的检查不重复运行。
4. 每个候选版本必须绑定提交、构建号、包内文件清单、验证记录与已知限制。源码、安装包、已安装应用、远程版本分别记录，不能互相替代。
5. 自动测试隐藏且不可聚焦，使用隔离数据；保留用户正在使用的 VPN、原配置和系统设置。长任务动态进度写入 work/STATE.md。
6. 对已授权可逆工作持续执行。需要管理员批准、实际切换用户网络、签名身份或正式发布时，先完成全部可安全准备的工作，并明确剩余具体条件。

## 当前发布门槛

| 门槛 | 验收标准 | 当前状态 |
|---|---|---|
| 功能与体验一致 | 主要任务闭环；保存/生效、失败/未知/恢复语义统一；主题、键盘、窄窗口及无障碍专项通过 | 2026-09-15 基线全套通过；后续变更按影响复验 |
| 数据与恢复 | 失败不损坏已有配置；升级/回滚/异常退出有可验证恢复路径 | 74d88ab 已通过隔离 macOS 的真实 root helper 与双 TUN/重装共 11 场景；用户机器和完整共存组合仍需验收 |
| 真实网络 | HTTP/SOCKS、DNS、规则、IPv4/IPv6、系统代理/TUN 与共存均有实际请求证据 | 手动模式有实网成功；间歇 TLS 错误未定位，IPv6及完整特权矩阵未完成 |
| 日常可靠性 | 连续运行、重复启停、真实睡眠与网络变化后恢复；无进程泄漏或第三方设置覆盖 | 25803c9 完成 30 分钟隔离手动模式检查；真实生命周期和日常时长仍待测 |
| 安装与更新 | 从干净环境安装；管理员流程明确；同版本升级/卸载；业务更新与完整应用更新各自验收 | 辅助服务隔离 CI 首次安装、失败替换保留、重装及重复卸载通过；原生静态更新目录解析通过，正式签名更新仍待验 |
| 产物可信 | 构建来源、签名后完整清单、依赖/许可证、包签名及隐藏启动/真实转发检查通过 | 签名后清单与精确源码归档已修复并通过针对性验证；正式签名/公证待条件具备 |
| 发布交付 | 精确候选提交的两项远程必需检查、版本说明、已知限制、下载产物与恢复说明一致 | 候选代码 74d88ab 在草稿 PR #12 的两项远程必需检查通过；后续提交需各自验证，尚未合并或正式发布 |

## 完成条件

正式发布前，以上必要门槛须有当前候选版本的通过证据；任何未满足项目必须明确影响和处理决定，不能以“已实现”替代验收。开发预发布应明确标记其范围和限制，不能把 ad-hoc 本地包标作已公证正式包。

最新持续准备证据：[2026-09-15 发布准备](READINESS_2026-09-15.md)；此前完整复核见 [复核报告](REVIEW_2026-09-15.md)。历史证据在各日期报告中保留；本文件定义长期标准，动态证据和下一步以 work/STATE.md 为准。

## 持续运行检查

`FLOWGATE_PACKAGE_DIR=/absolute/path/FlowGate.app npm run test:soak` 对指定包默认运行 30 分钟；`FLOWGATE_SOAK_SECONDS` 可指定 20–86400 秒，结果路径由 `FLOWGATE_SOAK_RESULT` 指定，默认 `work/soak-result.json`。检查使用全新隔离数据、隐藏窗口、真实本地 HTTP/SOCKS 转发、每分钟启停以及每两分钟页面重载；记录服务代次、内核退出、请求和应用内存采样。中断或失败不算通过。

它不进入普通 PR 的完整检查，避免将半小时检查塞入每次提交。时长与范围必须按结果如实报告：短测不代表日常长稳；本地回环不代表外网、真实睡眠/切网或特权模式。内存采样用于发现增长趋势，不能单凭数值宣布没有内存泄漏。

## 隔离机器上的特权验收

CI 的独立特权验收步骤使用生产本机安装器、XPC 与 root helper，在无既有辅助服务的临时 macOS 机器上测试系统代理、仅 `198.18.0.88/32` 的定向 TUN、正常停止及内核/helper 强杀后的恢复。系统代理请求使用 macOS URLSession 自动读取代理配置；helper 强杀后，在恢复断言之前不重新请求 helper，避免用服务重启掩盖 watchdog 的失败。最后通过生产卸载器清理，并核对代理、DNS 和默认路由。

此入口要求显式开关与 GitHub-hosted 环境，默认在本机拒绝执行。CI 通过也不代表用户 Mac 的管理员交互、所有 VPN 共存组合、整机 TUN、真实睡眠/切网或 Developer ID 分发已经完成。

新增双 TUN 验收使用两个独立内核、两个专用测试路由和互相拒绝对方目标的 HTTP 出口，检查 FlowGate 停止、另一内核退出重连及辅助服务崩溃时的相互隔离。该用例仍受同一临时 CI 守卫限制；以对应提交的实际结果为准。它验证原生/helper 层的定向共存，不代表 Service 的全部网络变化策略或任意 VPN 整机路由已经验收。
